<% if flash[:not_found] %>
  <p><%= flash[:not_found] %></p>
<% end %>

<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_songs_path(artist) %></li>
  <% end %>
</ul>
